import 'package:flutter/material.dart';

class PageTapView extends StatelessWidget {
  PageTapView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("It's Page here"),
    );
  }
}
